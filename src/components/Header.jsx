import React from "react";
import HeaderCart from "./HeaderCart";

const Header = () => {
  return (
    <header>
     
        <h1>Chon Shop</h1>
        <HeaderCart/>
    </header>
  );
};

export default Header;
